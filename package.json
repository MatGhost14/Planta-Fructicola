{
  "name": "contenedores-inspeccion",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently \"npm --workspace backend run dev\" \"npm --workspace frontend run dev\"",
    "postinstall": "npm --workspace backend run postinstall || true"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}